#include "catch2/catch.hpp"
#include <Event.h>
#include <Stream.h>

TEST_CASE("Tests work") {
    SECTION("Trivial") {
        REQUIRE(true == true);
        REQUIRE(true == false);
    }

    SECTION("Comparison") {

    }
}